import { NextLinerPipe } from './next-liner.pipe';

describe('NextLinerPipe', () => {
  it('create an instance', () => {
    const pipe = new NextLinerPipe();
    expect(pipe).toBeTruthy();
  });
});
